apiVersion: v1
kind: Service
metadata:
  name: inventario-service
spec:
  type: NodePort
  selector:
    app: inventario-app
    version: blue
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080